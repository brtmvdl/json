version: '3'

services:
  app:
    build:
      context: ./www
    ports:
      - '1200:80'
    volumes:
      - './www:/app'
  upload:
    image: tmvdl/node:surge
    working_dir: '/app'
    volumes:
      - '.:/app'
